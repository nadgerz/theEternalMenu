const BaseService = require('./base-service');
const VersionModel = require('../models/version');

class VersionService extends BaseService {
  // constructor() {
  //   // super(VersionModel, `${__dirname}/../user-database.json`);
  // }


}

module.exports = new VersionService();